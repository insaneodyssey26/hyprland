source = ~/.config/hypr/colors.conf

# Monitor
monitor=eDP-1, 1920x1080@144, 0x0, 1

# Apps
$terminal = kitty
$menu = wofi --show drun

# Volume Control
binde = , XF86AudioRaiseVolume, exec, wpctl set-volume -l 1.5 @DEFAULT_AUDIO_SINK@ 5%+
binde = , XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-

# Toggle Microphone Mute
bind = , XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle && notify-send "Microphone" "$(wpctl get-volume @DEFAULT_AUDIO_SOURCE@ | grep -q 'MUTED' && echo 'Muted' || echo 'Unmuted')" -t 1000

# Screen Brightness
binde = , XF86MonBrightnessUp, exec, brightnessctl set 5%+
binde = , XF86MonBrightnessDown, exec, brightnessctl set 5%-

# Keybinds
bind = SUPER, T, exec, $terminal
bind = SUPER, D, exec, $menu
bind = SUPER, Q, killactive
bind = SUPER, B, exec, brave
bind = SUPER, E, exec, thunar

# Press Super + H to "Hide" the active window into the scratchpad
bind = SUPER, H, movetoworkspacesilent, special:minimized

# Press Super + Shift + H to "Unhide" or see what's in there
bind = SUPER SHIFT, H, togglespecialworkspace

# Lock, wait 2 seconds, then turn off the screen
bind = SUPER, L, exec, hyprlock & sleep 2 && hyprctl dispatch dpms off

# Pause key kills screen, but waits 0.5s so your finger has time to lift off the key
bindl = , Pause, exec, pidof hyprlock && (sleep 0.5 && hyprctl dispatch dpms off)

# --- Master Screenshot Shortcuts ---

# [Print] -> Instant Full Screen

# Freezes, saves to your Screenshots folder, and copies to clipboard
bind = , PRINT, exec, grimblast --freeze copysave screen ~/Pictures/Screenshots/$(date +'%Y-%m-%d_%H-%M-%S').png

# [Super + Print] -> Surgical Edit (Active Window)
# Freezes, captures the active window, and opens Satty for editing
bind = SUPER, PRINT, exec, grimblast --freeze save active - | satty --filename -

# Workspaces
bind = SUPER, 1, workspace, 1
bind = SUPER, 2, workspace, 2
bind = SUPER, 3, workspace, 3
bind = SUPER, 4, workspace, 4
bind = SUPER, 5, workspace, 5
bind = SUPER, 6, workspace, 6
bind = SUPER, 7, workspace, 7
bind = SUPER, 8, workspace, 8
bind = SUPER, 9, workspace, 9

# Super + W to change wallpaper and sync colors
bind = SUPER, W, exec, ~/.config/hypr/scripts/random-wall.fish

# Move active window to a workspace with Super + Shift + [0-9]
bind = SUPER SHIFT, 1, movetoworkspace, 1
bind = SUPER SHIFT, 2, movetoworkspace, 2
bind = SUPER SHIFT, 3, movetoworkspace, 3
bind = SUPER SHIFT, 4, movetoworkspace, 4
bind = SUPER SHIFT, 5, movetoworkspace, 5
bind = SUPER SHIFT, 6, movetoworkspace, 6
bind = SUPER SHIFT, 7, movetoworkspace, 7

bindm = SUPER, mouse:272, movewindow
bindm = SUPER, mouse:273, resizewindow

# --- Window Navigation (Focus) ---
# Move focus with Super + Arrow keys
bind = SUPER, left,  movefocus, l
bind = SUPER, right, movefocus, r
bind = SUPER, up,    movefocus, u
bind = SUPER, down,  movefocus, d

# --- Window Manipulation (Move) ---
# physically swap window positions with Super + Shift + Arrow keys
bind = SUPER SHIFT, left,  movewindow, l
bind = SUPER SHIFT, right, movewindow, r
bind = SUPER SHIFT, up,    movewindow, u
bind = SUPER SHIFT, down,  movewindow, d

# Fullscreen
bind = SUPER, F, fullscreen

# Clipboard manager
bind = SUPER, V, exec, cliphist list | wofi --dmenu | cliphist decode | wl-copy

bind = SUPER, R, exec, wofi --show drun

exec-once = waybar
exec-once = awww-daemon
exec-once = awww restore
exec-once = /usr/lib/polkit-kde-authentication-agent-1
exec-once = wl-paste --type text --watch cliphist store # Stores only text data
exec-once = wl-paste --type image --watch cliphist store # Stores only image data
exec-once = walker --service
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = gnome-keyring-daemon --start --components=secrets
exec-once = hypridle

decoration {
    rounding = 10

    active_opacity = 1.0
    inactive_opacity = 0.95

    blur {
        enabled = true
        size = 6
        passes = 2
    }
}

animations {
    enabled = true

    bezier = myBezier, 0.05, 0.9, 0.1, 1.0

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%

    animation = fade, 1, 4, default
    animation = workspaces, 1, 5, default
}

general {
    gaps_in = 4
    gaps_out = 5
    border_size = 2
    col.active_border = $primary 
    col.inactive_border = $surface
}

misc {
    mouse_move_enables_dpms = true
    key_press_enables_dpms = true
}

input {
    kb_layout = us
    numlock_by_default = true

    touchpad {
        natural_scroll = true
        disable_while_typing = true
    }

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
}
